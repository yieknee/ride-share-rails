<div>
  <h2>Passenger Name: <%= @passenger.name %></h2>
  some image
  <h3>Phone_number: <%= @passenger.phone_num %> </h3>
  <h3>Total_charged: <%= @passenger.total_charged.nil? ? "N/A": @passenger.total_charged %></h3>

  <button class='tablink'><%= link_to "Edit Passenger", edit_passenger_path(@passenger) %></button>
  
  <button class='tablink'> <%= link_to "Delete Passenger", passenger_path(@passenger), method: :delete, data: {confirm: "Are you sure?"} %>
  <button class='tablink'> <%= link_to 'Create a New Trip', trips_path(@passenger.id), method: :post %></button>
   
</div>
<div>
  <h2><%= @passenger.name %>'s Trips</h2>
  <table>
    <tr>
      <th>Trip ID</th>
      <th>Trip Date</th>
      <th>Passenger Name</th>
      <th>Driver Name</th>
      <th>Rating</th>
      <th>Cost</th>
    </tr>
    
    <% @trips.each do |trip| %>
    <tr>
      <th><%= link_to trip.id, trip_path(trip.id) %></th>
      <th><%= trip.date %></th>
      <th><%= link_to "#{@passenger.name}", passenger_path(@passenger.id)%></th>
      <th><%= link_to "#{Driver.find_by(id: trip.driver_id).name}", driver_path(trip.driver_id)%></th>
      <th><%= trip.driver_id %></th>
      <th><%= trip.rating %></th>
      <th><%= trip.cost %></th>
    </tr>
    <% end %>
  </table>
</div>

