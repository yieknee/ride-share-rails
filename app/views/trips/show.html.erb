<div>
  <h2>Trip Details: 
  <h3>Trip ID: <%= @trip.id %> </h3>
  <h3>Trip cost: <%=@trip.cost %></h3>
  <h3>Trip rating: <%=@trip.rating %></h3>
  <h3>Passenger Name: <%= link_to "#{Passenger.find_by(id: @trip.passenger_id).name}", passenger_path(@trip.passenger_id)%>
  <h3>Driver Name: <%= link_to "#{Driver.find_by(id: @trip.driver_id).name}", driver_path(@trip.driver_id)%>
  <br>
  <button class='tablink'>Edit trip</button>
  <button class='tablink'>Delete Trip</button> 
   
</div>